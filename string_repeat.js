//Write a function called repeatStr which 
//repeats the given string string exactly n times.

function repeatStr (n, s) {
    return s.repeat(n);
  }

let value = repeatStr(4, 'Hello')
console.log(value)
